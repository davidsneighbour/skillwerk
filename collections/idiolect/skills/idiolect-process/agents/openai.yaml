interface:
  display_name: "Idiolect process"
  short_description: "Create prose in the established personal voice"
  default_prompt: "Use $idiolect-process to build a voice profile, check a draft against it, or rewrite a draft in the author's voice."
