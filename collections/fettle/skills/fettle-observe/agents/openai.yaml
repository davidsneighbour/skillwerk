interface:
  display_name: "Fettle Observer"
  short_description: "Find patterns in agent executions"
  default_prompt: "Use $fettle-observe to analyse available agent execution evidence and report recurring, deduplicated findings."
